<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Sweatify"
        tools:targetApi="31">
        <activity
            android:name=".SplashActivity"
            android:exported="true"> <!-- Make sure this is exported as it is the entry point -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="true" /> <!-- Adjusted to true if you plan to launch it from SplashActivity -->
        <activity
            android:name=".WaterActivity"
            android:exported="false" />
        <activity android:name=".CaloriesActivity" />
        <activity android:name=".ExersiceActivity" />
        <activity android:name=".SleepActivity" />


    </application>
</manifest>
